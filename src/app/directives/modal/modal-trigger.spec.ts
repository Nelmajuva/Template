import { ModalTrigger } from './modal-trigger';

describe('ModalTrigger', () => {
  it('should create an instance', () => {
    const directive = new ModalTrigger();
    expect(directive).toBeTruthy();
  });
});
